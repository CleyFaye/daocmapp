#!/bin/bash
# I know it's awfull, but i find some dds in dx5, texconv.exe come from de DX9SDK from $soft so i work under windows like many who play daoc
#but whith cygnus for dev
../bin/texconv.exe -nologo -f R8G8B8 -ft BMP -if NONE -o . -m 1 temp.dds >nul
